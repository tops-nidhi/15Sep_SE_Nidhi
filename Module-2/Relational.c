#include<stdio.h>
void main()
{
    int a,n;
    printf("Enter value of a and n:");
    scanf("%d%d",&a,&n);
    if (a != n)
    {
        printf("\nBoth are not equle.");
    }
    else
    {
        printf("\nBoth are equle.");
    }
}